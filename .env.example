# Copy to .env and fill in. Real .env is gitignored.

ANTHROPIC_API_KEY=sk-ant-

DB_CONNECTION_STRING="Host=localhost;Port=5433;Database=packetready;Username=packetready;Password=packetready"

# Self-hosted Langfuse via docker-compose. After `docker compose up -d`, visit
# http://localhost:3000, create an account + project, copy keys here.
LANGFUSE_PUBLIC_KEY=pk-lf-
LANGFUSE_SECRET_KEY=sk-lf-
LANGFUSE_OTEL_ENDPOINT=http://localhost:3000/api/public/otel/v1/traces
